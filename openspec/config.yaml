schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
context: |
  Tech stack: HTML5, CSS3 (Vanilla, Flexbox, UI/UX Sci-fi Game), JS (Vanilla, ES6 modules).
  Database: Supabase (PostgreSQL, Realtime).
  Fallback: LocalStorage is used as a fallback if Supabase is unavailable.
  Domain: Gamified E-learning platform for Primary School (Math & Vietnamese).
  State Management: Singleton `window.app` object.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
